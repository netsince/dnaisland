# 网页版(手机·电脑) vs Flutter 客户端 — 交互/UI 布局/校验 严肃对比审计

> 范围：全站用户侧功能（首页/探索/推荐/搜索、角色卡详情/发布、茶馆、个人中心/资料/通知/工单/处罚/点数、生图/文章/评论）。
> 说明：侧边栏 + 底栏导航为客户端已确定的专属形态，不做对齐；其余交互差异全部列出。
> 方法：逐文件对照 Flask 模板/路由 与 Flutter 页面，每条给「网页版 / 客户端 / 差距」。

---

## 0. 全局差异（跨功能）

| 维度 | 网页版 | 客户端 | 差距 |
|---|---|---|---|
| 导航 | 顶部导航栏(桌面) + 移动端专用底部/顶部工具组 | 侧边栏(横屏) + 底栏(竖屏) **（已定，专属）** | 不要求对齐 |
| 分页模型 | 几乎全部用**页码翻页**（整页跳转） | 几乎全部**无限滚动** + 下拉刷新 | 交互范式不同，但各成体系、体验都可接受 |
| 加载态 | 服务端整页渲染，大多无骨架屏 | shimmer 骨架屏 + 淡入 | 客户端更优 |
| 空态/错误态 | 大多只有空态文本，无错误重试 | 统一 `EmptyState` + `ErrorState`(可重试) | 客户端更优 |
| 未登录策略 | 大多路由级 `@login_required` 整页跳登录 / 直接隐藏按钮 | 页内软占位 + 交互时 SnackBar 拦截 | 客户端交互更轻，但无跳登录引导 |
| 上传通道 | multipart 文件直传 + 服务端压缩/转 WebP | JSON base64 data-URL 上传 | 大图上传客户端耗流量/易超时 |
| 主题/外观 | `data-bs-theme` 深色/浅色切换 | 客户端外观设置 | — |

---

## 1. 首页「为你推荐」

- **Hero 区**：网页有 Hero（标题/副标题/按钮，站点可配开关）；客户端 `HomeBody` 直接渲染，无开关，另有首启公告底部弹层（网页是模态框）。
- **封面比例**：网页方形缩略；客户端 **3:4 竖版**。
- **信息组织**：网页卡片显示名称+「性别·浏览·复制」一行，不显示作者；客户端名称+性别徽标、作者单独一行。网页不展示作者。
- **「换一换」**：网页标题旁有显式「换一换」按钮（`?exclude=已见id` 无刷新换一批）；客户端无此按钮，靠下拉刷新+无限滚动。
- **「刷一刷」**：客户端独有全屏左右滑页，网页无等价物。
- **卡片互动**：两端卡片都仅整卡可点进详情，无内嵌收藏/点赞/隐藏。

## 2. 探索

- **筛选**：两端都有「性别+排序」与「标签」两行横划胶囊栏，排序 key 一致(hot/new/likes)。网页「清除筛选」是标题行右侧按钮；客户端是筛选行首部胶囊，且点已选性别/标签可 toggle 取消（网页需点清除）。
- **网格**：两端都是响应式网格（手机 2 列→桌面 5~6 列）。
- **分页**：网页页码翻页；客户端无限滚动+下拉刷新。

## 3. 站长推荐

- **头部**：网页有醒目的 rec-hero 面板（标题+副标题）；客户端只有 AppBar 标题，无副标题。
- **瀑布流**：网页按 CSS 断点 1~4 列，手机端封面改 16:10；客户端按宽度 2~6 列+单列，封面恒 3:4。
- **创作者卡**：网页带验证徽章、推荐语在卡片下方；客户端推荐语内嵌、带 chevron，无验证徽章。

## 4. 全站搜索（差异最大）

- **入口**：网页在全局导航栏内联搜索框（200ms 建议）；客户端是独立搜索页（300ms 建议）。
- **类型 Tab**：网页「全部/角色卡/茶馆动态/作者」各带计数 badge、AJAX 无刷新+浏览器历史；客户端 Tab 无计数、无 URL 语义。
- **排序**：网页有「相关度/最热/最新」；客户端**无任何排序**。
- **「全部」视图**：网页含作者 spotlight + 各板块计数与「查看全部」深链 + 网格卡片；客户端是纯列表、无 spotlight、无深链。
- **分页**：网页各类型可翻页；客户端**只加载第 1 页，无法翻页/加载更多**（搜索最大缺口）。
- **作者结果**：网页带关注按钮；客户端无关注。
- **茶馆帖结果**：客户端内嵌点赞/收藏（未登录弹提示）；网页只有「查看回复」。

---

## 5. 角色卡详情

### UI 布局
- **信息组织**：网页「左资料栏(sticky)+右内容」双栏，作者只是一个 chip；客户端多出独立「作者板块」（粉丝/卡数/关注/跳主页），是客户端多出的能力。
- **封面**：网页**设封面焦点后才 cover 裁切、否则原比例完整展示**；客户端 hero 恒 16:9 cover 裁切——**`coverEnabled` 逻辑写了但 `_HeroBanner` 没用上，封面焦点在详情页未真正生效**。
- **开场白**：网页斜体；客户端普通样式。
- **发布表单**：网页对话风格「用户/角色」纵向堆叠、图片三槽位横排；客户端对话风格并排一行、图片槽位纵向单列。客户端多出「原创链接」输入框（网页版无）。

### 评论区（客户端明显偏弱）
- 网页支持：评论图片上传/查看、@提及补全、500 字计数、「回到最新」、`?comment=id` 深链定位、赞助者红星、审核中徽章、楼中楼递归折叠(>3条)。
- 客户端：图片仅显示 `[图片]` 占位（不能看/传）；无 @补全、无 500 计数、无回到最新、无深链、无星标/审核徽章、楼中楼硬限两层。
- 未登录：网页评论区 footer 有「登录后参与评论」链接；客户端仅输入框文案，无跳登录入口。

### 导出/复制
- 网页复制成功有按钮反馈动画，未登录有 confirm 二次确认跳登录；客户端仅 snack「请先登录后再复制」。客户端多出「复制网页链接」（网页版无）。

### 发布入口引导
- 网页有独立 start 页（剪贴板识别/JSON/直接编辑三选一），支持**读剪贴板一键识别**；客户端只有手动粘贴到 JSON 输入框再点导入，无独立引导页。

### 我的角色卡
- 网页有「待审/已过/已拒」统计头部、列表项带封面缩略图；客户端无统计头部、列表无封面缩略图。客户端有下拉刷新与空态「去上传」引导（网页无）。

### 发布表单校验
- 两端都只有「名称」硬必填、性别默认「无性」。
- 标签删除：网页支持 Backspace 空输入删最后一个；客户端只能点 ×。
- 提交健壮性：网页网络失败自动重试一次+幂等去重提示；客户端单次提交无重试。

---

## 6. 茶馆

### UI 布局
- **原帖链**：网页横向面包屑（作者名串联、赞助者红星、「你在这」）；客户端纵向小卡列表（带头像+正文预览），链上无星标、主帖无「原帖」标签。
- **话题徽章**：网页可点跳话题页；客户端只弹 SnackBar 不跳转（但客户端独有话题列表/话题详情两级页）。
- **回复区**：网页有「最新/最热」排序切换 + 「回复 @某某」上下文小字；客户端无排序 UI、无引用上下文。
- **发帖入口**：网页抽屉(不跳页)；客户端独立发帖页 + 详情底部常驻回复栏。
- **长文**：网页卡片可「展开全文」；客户端固定 6 行省略，要进详情。

### 细节体验
- 未登录：网页 feed 隐藏发帖入口+提示条；客户端始终显示 FAB、提交时才拦。
- 编辑弹层：网页可改关联卡；客户端编辑弹层仅正文+话题+移除配图，**缺表情包按钮**。
- 表情包：客户端发帖/回复有按钮；网页靠注入。

### 校验
- **280 上限**：客户端**回复输入框缺 maxLength**(靠后端兜底)；发帖/编辑有 280。
- 话题：网页后端截断 50 字；客户端无前端限制。
- 配图：两端一致(单图/1280/82)，网页服务端统一转 WebP。
- 编辑/软删：两端都依赖后端 15 分钟窗口。
- 两端都未渲染「转发原帖」UI（模型有 quote_post_id 但两端都没用）。

---

## 7. 个人中心

### 个人主页
- **布局**：网页桌面双栏；客户端单列+吸顶 TabBar。
- **特殊态**：网页有「纪念/账号删除/主页受限」三种状态；客户端完全没有处理。
- **字段**：网页展示网站/生日；客户端头卡不展示。
- **他人页**：网页有「举报」按钮；客户端无举报，但多出「复制主页链接」「BYOK 代理配置」。
- **Tab**：网页 4 个(角色卡/茶馆帖子/茶馆回帖/角色卡评论)；客户端仅 3 个，**茶馆 tab 是空态占位、无回帖 tab**。

### 资料编辑
- **头像**：网页 cropper 强制 1:1 方形裁剪 + multipart；客户端 ImagePicker 无裁剪、base64 上传。
- **校验**：网页昵称 maxlength=80/简介 500/网站 type=url 自动补 https；客户端昵称/简介无长度限制、网站无 URL 校验。

### 关注/收藏/点赞
- 网页关注/粉丝列表页可内联互关；客户端列表页无关注按钮。
- 网页收藏/点赞独立分页页；客户端复用同一组件滚动加载。

### 通知
- 网页绝对时间、不可点击跳转；客户端相对时间、可点进卡片详情。

### 工单
- 网页支持主题搜索；客户端无搜索(有最后消息预览、FAB 新建)。
- 网页气泡带头像、图片可放大；客户端 IM 式左右气泡无头像。

### 处罚
- 网页内联展示申诉审核结果(已通过/已拒绝/审核中)；客户端仅「已提交申诉」+ 底部弹层申诉。

### 点数
- 网页表格+分页+`?code=`回填；客户端 ListTile 滚动加载+结果卡片+SnackBar 汇总+获取积分外链。

---

## 8. 生图工作台

- **布局**：网页「左表单+右历史」横向两栏；客户端「表单在上、历史在下」纵向单栏。
- **宽高比**：网页下拉面板带形状图示；客户端平铺 ChoiceChip 无图示。
- **参考图**：网页支持拖拽+去重、原样上传；客户端走系统相册、选择时压缩(1280/82)、无拖拽、无「已选满5张」提示。
- **轮询**：网页 3.5s+骨架卡片+页面隐藏暂停；客户端 4s+仅文字提示、未做暂停。
- **下载**：网页有「WebP→PNG 下载原图」；**客户端完全不能下载/保存**。
- **同款生成/复制 Prompt/详情参数/参考图/消耗/失败原因**：网页详情页齐全；**客户端只看图片查看器，全部缺失**。
- **点数不足**：网页专属模态引导「去获取积分」；客户端仅 SnackBar。
- **未登录**：网页路由级跳登录；客户端页内占位+软拦截。
- **提示词校验**：网页 `trim` 判空；客户端只 `isEmpty`（空白提示词可能放行到后端再拦）。

## 9. 官方文章

- 网页多列卡片网格+数字分页+作者徽章(区分匿名管理员)；客户端单列 ListTile+无限滚动+相对时间。
- 富文本：网页服务端 `richtext` 自定义渲染；客户端通用 `flutter_html`，样式能力可能不一致。

## 10. 评论(卡片)

- 逻辑共用 `toggle_relation`；网页 flash+就地切换；客户端乐观更新+失败回滚。
- 网页支持评论图片上传(格式/≤5MB/转 WebP 校验)；客户端只显示 `[图片]` 占位、**无上传能力**。
- 空评论：网页有明确「内容不能为空」；客户端空文本直接 return 无提示。

---

## 11. 客户端可拆分复用组件清单

> 目标：识别客户端可单独拆出、跨页复用的组件。只列候选，不写代码。

### A. 已复用良好（已抽象为 widgets/ 下组件）
- `card_tile.dart` — 角色卡卡片（首页/探索/推荐/搜索/我的卡 复用）
- `comment_section.dart` — 评论区（含排序/只看作者/楼中楼/置顶/删除/表情包，靠注入回调，卡详情复用）
- `teahouse_post_tile.dart` — 茶馆帖子卡片（feed/详情/搜索/话题复用）
- `state_views.dart`(EmptyState/ErrorState)、`load_more_list.dart`、`refreshable.dart`、`avatar.dart`、`sticker_text.dart`、`fade_in_image.dart`、`image_viewer.dart`、`async_action_button.dart`、`shimmer.dart`

### B. 明显重复、应拆出复用的候选（重点）
1. **点赞/收藏 toggle 逻辑** — 在 `teahouse_body`(×2)、`teahouse_topics_page`、`teahouse_post_page`、`search_page` 重复实现「登录守卫 + 乐观更新 + SnackBar 提示」，约 5~6 处几乎相同的 `_toggleLike/_toggleFavorite`。应抽成共享 mixin/helper（如 `PostLikeFavMixin`）或一个可注入回调的状态组件。
2. **未登录拦截 SnackBar** — 各处散落 `_snack('请先登录后再…')`，可统一为 `AuthGuard.tryRun(context, action)` 帮助方法。
3. **分页无限滚动 + 加载中/到底 状态** — `LoadMoreListView` 已存在，但部分列表仍是手写 ScrollController+`_loadMore`，应统一收敛到该组件（探索/我的卡/收藏/通知/点数/生图历史/工单）。
4. **卡片网格 SliverGrid 自适应列数** — 首页/探索/我的卡/收藏/生图历史都算列数，可抽 `ResponsiveCardGrid`（含 shimmer 骨架）。
5. **作者/资料头卡块** — me_page 与他人主页共用，含头像/昵称/@用户名/统计/关注/编辑/举报/复制链接，可抽 `ProfileHeaderCard`。
6. **相对时间** — 已抽 `utils/time_format.dart`，良好。
7. **标签 chips 输入** — 卡发布页已有 `_TagInput`(InputChip)，是内嵌私有类；可上提为 widgets/ 共享组件供茶馆话题/搜索标签场景复用。
8. **对话风格编辑器** — 卡发布用，可拆 `DialogueStyleEditor`。
9. **图片裁剪页** — 已抽 `card_crop_page.dart`；资料编辑头像裁剪目前没复用它（网页头像走 1:1 方形裁剪），可复用到头像场景。
10. **关注/粉丝列表页** — 已共享 `FollowListPage`，良好。

### C. 网页有、客户端无、需新增或对齐能力的（不算拆分，但列全）
- 评论图片上传/查看、@提及补全、500 字计数、回到最新、深链定位、楼中楼折叠
- 封面焦点在详情展示真正生效
- 生图下载、同款生成、复制 Prompt、独立详情页
- 搜索排序/分页/作者关注/spotlight
- 茶馆回复排序、话题徽章跳转
- 我的角色卡统计头部
- 个人主页特殊态(纪念/删除/受限)、举报入口
- 头像 1:1 方形裁剪

---

## 12. 优先级建议（影响排序）与修复状态

> ✅ = 已修复（客户端实现并验证）；⬜ = 未做（结构重构/低影响，可选）

1. **P0 明显缺陷**
   - ✅ 评论区图片只能看占位 → 已支持上传(相册压缩)/展示/全屏查看，后端返回 `image_data`
   - ✅ 生图完全不能下载 → 全屏查看器加「下载原图」(带认证拉取存临时目录)
   - ✅ 封面焦点在详情未生效 → 头图按焦点坐标 cover 定位，未设焦点完整展示原图
   - ✅ 搜索无法翻页 → 三种结果滚动到底自动加载下一页，角色卡加排序(相关度/最热/最新)，用户结果加关注
   - ✅ 回复输入框缺 280 上限 → 已补 `maxLength:280`；评论输入补 500 字数计数
2. **P1 体验对齐**
   - ✅ 发布入口剪贴板一键识别 → AppBar「从剪贴板识别」菜单
   - ✅ 未登录统一跳登录引导 → 新增 `AuthGuard`，SnackBar 带「去登录」切到「我」页
   - ✅ 提交失败重试/去重提示 → 网络失败自动重试一次
   - ✅ 点数不足引导 → 生成前预校验，余额不足弹「去获取积分」
   - ✅ 茶馆长文就地展开 → 正文 >200 字可「展开全文/收起」
3. **P2 复用重构**
   - ✅ 点赞/收藏 toggle 抽共享组件 → 新增 `PostLikeFavMixin`，茶馆 Feed 主列表已用
   - ✅ 未登录守卫抽 helper → `AuthGuard`(评论/卡片详情/搜索 已接入)
   - ✅ 头像裁剪复用到资料编辑 → 资料编辑头像走 `card_crop_page` 强制 1:1 方形裁剪
   - ⬜ 分页收敛到 LoadMoreListView（结构重构，功能已正常，可选）
   - ⬜ 卡片网格抽 ResponsiveCardGrid（结构重构，功能已正常，可选）
4. **P3 锦上添花**（未做）
   - ⬜ 生图同款生成 / 茶馆回复排序 / 工单搜索
