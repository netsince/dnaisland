# DNAISLAND：网页版 vs 客户端（Flutter App）功能差异

> 对比对象：
> - **网页版**：Flask 后端渲染的 Web 前端（`app/routes/*` + `app/templates/*`）
> - **客户端**：Flutter 应用 `dnaisland-app/lib/*`，通过 `/api/v1/*` JSON 接口与后端通信
>
> 结论先行：**客户端已覆盖网页版绝大多数核心功能**。角色卡浏览/探索/详情/点赞/收藏/评论、用户主页、关注、通知、点数、处罚、官方文章、站长推荐、赞助、茶馆、上传（发布/编辑角色卡）、搜索、我的角色卡管理、个人资料编辑、我的工单等已覆盖。
>
> **有意保留、暂不补齐（非遗留项）**：
> 1. **站内注册（邮箱验证码流程）** —— 有意保持「跳转网页版注册」，避免在客户端重复实现带来后续维护成本；
> 2. **法律协议页（`/privacy`、`/tos`）** —— 有意不补（APP 内以系统浏览器打开网页协议页）。
>
> 其余「网页有、App 没有」仅为小细节（如「刷一刷/换一批」的本地打乱），详见下表。

---

## 一、总览：客户端已实现 vs 未实现

| 功能域 | 网页版 | 客户端 | 说明 |
| --- | --- | --- | --- |
| 首页推荐 / 为你推荐 | ✅ | ✅ | 两端共用 `featured_cards` |
| 探索（热门/最新/按赞/性别/标签筛选） | ✅ | ✅ | 两端共用 `explore_cards` |
| 角色卡详情 / 点赞 / 收藏 | ✅ | ✅ | 共用 `card_detail_core` |
| 角色卡作者注释 / 更新时间展示 | ✅ | ✅（本次新增） | 卡详情现展示 authorNote + 注入间隔 + updated_at |
| 角色卡评论（楼中楼/置顶/点赞/删除） | ✅ | ✅ | 共用 `card_comments_list` / `create_comment` |
| 复制/导出角色卡 | ✅ | ✅ | 共用 `card_export_package` |
| 用户主页 / 关注 / 粉丝列表 | ✅ | ✅ | 共用 `profile_cards` |
| 官方文章（公告） | ✅ | ✅ | 共用同一查询 |
| 站长推荐 | ✅ | ✅ | 共用 `recommend_items` |
| 赞助页 | ✅ | ✅ | 共用 `sponsors` |
| 通知（含未读、全部已读） | ✅ | ✅ | 共用 `notifications_page` |
| 点数中心（明细 + 兑换） | ✅ | ✅ | 共用 `redeem_codes` / `point_transactions` |
| 我的处罚 + 申诉 | ✅ | ✅ | 共用 `punishment_service` |
| 举报 | ✅ | ✅ | 共用 `report_service`（卡详情内） |
| 站点配置 / 关站 / 公告 | ✅ | ✅ | 共用 `site_config` |
| **茶馆（论坛）** | ✅ | ✅（已补齐） | 客户端底栏茶馆现已完整实现：Feed/详情/发帖/回复/点赞/收藏/话题/我的收藏/编辑/删除 |
| **上传 / 发布角色卡** | ✅ | ✅（已补齐） | 客户端底栏「上传」打开新建表单：正文+三槽位图片+对话风格+作者注释+标签+JSON导入 |
| **生图（AI 图像生成）** | ✅ | ✅（已补齐） | 底栏「生图」接入真实工作台（提示词/模型/张数/宽高比/参考图/历史记录） |
| **表情包（Sticker）** | ✅ | ✅（已补齐） | 评论/茶馆输入带表情选择器，正文 `[sticker:CODE]` 内联渲染 |
| **搜索** | ✅ | ✅（已补齐） | 顶栏搜索现已可用：支持 角色卡/用户/茶馆帖 全站搜索 |
| **搜索实时建议** | ✅ | ✅（已补齐） | 顶栏输入框实时下拉建议（角色卡/作者），走 `/api/v1/search/suggest` |
| **刷一刷 / 换一批** | ✅ | ❌ | 网页首页「换一批」打乱；APP 顶栏「刷一刷」为**占位页** |
| **管理后台** | ✅ | ❌（不在本次统计范围） | 仅网页可用，客户端无 |
| **我的工单** | ✅ | ✅（已补齐） | 我的页新增工单入口：列表/新建（可附图）/详情对话/回复/关闭/重开 |
| **个人资料编辑** | ✅ | ✅（已补齐） | 我的页「编辑资料」：昵称/简介/所在地/网站/生日/点赞通知 + 头像上传/移除 |
| **我的角色卡 / 管理 / 编辑 / 重新提审** | ✅ | ✅（已补齐） | 我的页新增管理入口：状态徽章 + 编辑/重新提审/隐藏 |
| **站内注册（邮箱验证码）** | ✅ | ✅（走网页版） | 有意保持跳转网页注册，不在客户端重复实现 |
| 法律协议（隐私 / TOS） | ✅ | ✅（走浏览器） | 有意不补，用系统浏览器打开网页协议页 |
| **代理（BYOK）** | ✅ | ✅（已补齐） | 个人页入口 + 配置页（查看/编辑/重置令牌/删除，展示对外 base URL 与令牌） |
| **外链中转（`/teahouse/leave`）** | ✅ | ✅（已补齐） | 点任意外部链接先弹「即将离开」确认（即将跳转浏览器），正文 URL 自动识别为可点击链接 |

---

## 二、网页版有、但客户端没有的功能（详细清单）

### 1. 茶馆（论坛）—— 客户端现已完整实现
- ✅ **已补齐（客户端）**：底栏「茶馆」现已可用，实现：
  - Feed（热门/最新）+ 下拉刷新 + 无限分页
  - 帖子详情（主帖 + 原帖链 + 回复分页）、点赞/收藏/回复
  - 发帖/回复：正文 + 单图（image_picker）+ 话题 + 关联角色卡
  - 编辑/删除自己的帖子
  - 话题列表 + 话题详情（分页）、我的收藏列表（分页）
  - 卡片详情「引用到茶馆」
- 注：网页版 Feed 的「只看关注/只看粉丝/随机」排序为 Web 专属，App 版 `_teapost_item` 接口仅暴露 hot/new，客户端对齐 App 接口。

### 2. 上传 / 发布角色卡 —— 客户端现已完整实现
- ✅ **已补齐（客户端）**：底栏「上传」打开新建角色卡表单（`card_publish_page.dart`），实现：
  - 名称（必填）/ 性别 / 人设 / 简介 / 开场白 / 原创链接 / 封面焦点 / **seed** / **作者注释 + 注入间隔** / 标签
  - 三槽位图片（square/landscape/portrait，image_picker）
  - 对话风格增删（user/assistant 列表）
  - **从导出的 JSON 导入预填**（`parseCardImport`）
  - 提交走 `/api/v1/cards/publish`（新建）或 `/api/v1/cards/<id>/edit`（编辑，自动重新提审）

### 3. 角色卡管理（我的角色卡）—— 客户端现已实现
- ✅ **已补齐（客户端）**：我的页新增「我的角色卡」快捷入口（`my_cards_page.dart`）：
  - 分页列出自己的角色卡，展示审核状态徽章（已通过/已拒绝/审核中）+ 已隐藏
  - 每张卡操作：查看详情 / 编辑（复用发布页）/ 重新提审（仅被拒绝）/ 切换隐藏
  - 空态提供「去上传」入口
- 后端 `/api/v1/my/cards` 现随 `_card_light` 返回 `status` 与 `is_hidden`（见 `app/routes/api.py`）。

### 4. 编辑个人资料 —— 客户端现已实现
- ✅ **已补齐（客户端）**：我的页新增「编辑资料」按钮（`profile_edit_page.dart`）：
  - 昵称 / 简介 / 所在地 / 网站 / 生日（日期选择）/ **点赞通知开关**
  - **头像上传**（image_picker → base64）与 **移除头像**
  - 对接 `GET/POST /api/v1/me/profile`（GET 用于预填，POST 保存后自动刷新资料）

### 5. 搜索 —— 客户端现已实现
- ✅ **已补齐（客户端）**：顶栏搜索按钮打开全站搜索页，支持 **全部 / 角色卡 / 用户 / 茶馆帖** 四个 Tab。
- 角色卡结果用 CardTile 网格、用户结果含头像/认证/跳转作者主页、茶馆帖结果用帖子卡片；对接后端 `/api/v1/cards/search`、`/users/search`、`/teahouse/search`。

### 6. 生图（AI 图像生成工作台）—— 客户端现已实现
- ✅ **已补齐（客户端）**：底栏「生图」接入真实工作台（`image_gen_body.dart`）：
  - 提示词、模型选择、张数（1/2）、宽高比、参考图（最多 5 张）与点数展示；
  - 提交生成任务后轮询完成状态，失败/成功提示；
  - 「我的生成记录」历史网格 + 全屏查看器（带认证头加载产出图）。
- 后端新增 `/api/v1/image-gen/*`（meta / generate / tasks / logs），产出图/参考图接口兼容 App JWT 访问。

### 6b. 表情包（Sticker）—— 客户端现已实现
- ✅ **已补齐（客户端）**：`lib/widgets/sticker_text.dart` 新增 `StickerCatalog`（内存缓存 `code→image_url`，幂等加载 `/stickers/api`）、`StickerText`（把正文 `[sticker:CODE]` 渲染为内联表情图，未知码回退原文本）与 `StickerPickerSheet`（按系列分组的表情选择底部弹层）。
- 接入点：
  - 评论区输入框、茶馆回复框、茶馆发帖框均加「表情包」按钮，选中后在光标处插入 `[sticker:CODE]`；
  - 评论内容、茶馆帖/回复/原帖链、个人主页「我的评论」统一用 `StickerText` 渲染 `[sticker:CODE]`。
- 后端 `/stickers/api` 为公开 JSON 接口，客户端 `ApiClient.getStickers()` 直接接入。

### 7. 我的工单（客服）—— 客户端现已实现
- ✅ **已补齐（客户端）**：我的页新增「我的工单」快捷入口（`tickets_page.dart`）：
  - 工单列表：状态筛选（全部/待处理/已回复/已关闭）+ 分页 + 下拉刷新
  - **新建工单**：分类下拉 + 主题 + 描述 + 可选图片
  - **工单详情**：对话气泡 + **回复（可附图）** + **关闭 / 重新打开**
- 后端新增 `/api/v1/tickets*` 全套接口（列表/类别/新建/详情/回复/关闭/重开）。

### 8. 管理后台 —— 仅网页
- 网页版 `admin_bp`（`/admin/*`）约 **80+ 个路由**：仪表盘、用户管理（创建/编辑/删除/点数/资料抽屉/快捷操作/封禁）、兑换码、生图模型、贴纸管理、图片/代理日志、复制统计、站长推荐、处罚与申诉处理、角色卡审核（单条/批量）、举报处理、评论审核、茶馆审核与管理、系统配置、官方文章、工单处理与分类、赞助管理、数据看板与趋势等。
- 客户端**没有任何管理能力**。

### 9. 法律协议页 —— 有意不补
- 网页版 `/privacy`、`/tos` 独立页面。客户端**有意不补**：用户需要时用系统浏览器打开网页协议页即可，避免重复维护一份协议内容。

### 10. 代理与链接中转 —— 客户端已补齐
- ✅ **BYOK 代理配置（`/proxy/set`）**：个人页新增「BYOK 代理配置」入口（`proxy_config_page.dart`），支持查看当前配置、编辑上游 Base URL / API Key / 备注 / 启用状态、重置访问令牌、删除配置，并展示可复制的对外 Base URL 与访问令牌。后端新增 `/api/v1/proxy/config`（GET/POST）。
- ✅ **外链中转（`/teahouse/leave`）**：新增 `lib/utils/external_link.dart` 的 `confirmOpenBrowser`，点击任意外部链接先弹「即将离开 DNAISLAND」确认（提示将跳转至浏览器），确认后才打开；统一接入 about / 卡详情 / 首页 / 个人页 / 积分 / 赞助等入口，`StickerText` 自动识别正文 URL 并渲染为可点击链接（点链接同样走确认）。

### 11. 角色卡详情里网页独有的内容
- 网页版卡详情展示 **「关联茶馆帖」列表**（linked_posts）与完整评论区（含楼层号定位）。
- 客户端卡详情**不展示关联茶馆帖**；评论区已实现（楼中楼/置顶/点赞/删除）。
- ~~网页版支持 `author_note`/`author_note_interval`（作者注释）并做展示，后端 API 已返回 `authorNote`/`authorNoteInterval`，但**客户端模型未解析、不展示**。~~
  - ✅ **已补齐**（客户端）：卡详情现已展示 **作者注释（authorNote + 注入间隔）** 区块与 **更新时间（updated_at）** 徽标（仅在更新日期与创建日期不同时显示）。见 `dnaisland-app/lib/card_detail_page.dart` 的 `CardDetail` 模型与 `_AuthorNoteSection` 组件，以及配套测试。

### 12. 其他细节差异
| 功能 | 网页版 | 客户端 |
| --- | --- | --- |
| 注册（邮箱验证码流程） | ✅ 站内完整注册（发送验证码 → 校验 → 建号） | ✅ 有意走网页版：登录页「注册」**跳转浏览器打开网页注册** |
| 记住登录 / 会话 | Cookie Session + JWT | JWT（本地持久化），无「记住我」选项 |
| 头像 | 网页上传/裁剪 | ✅ 编辑资料可上传/移除（内嵌 base64） |
| 顶栏实时搜索建议 | ✅ | ✅ 已补齐（顶栏输入实时下拉建议） |
| 版权/协议链接 | ✅ | ❌ |
| 站点「公告」展示 | ✅ 首页/顶栏 | ✅ 客户端也有（弹层） |
| 自定义服务器地址 / 外观（主题色、深浅色） | 无此概念（跟随站点） | ✅ 客户端**独有**（OOBE 配置服务器、强调色、明暗主题） |

---

## 三、客户端独有 / 优于网页的功能
为公平起见，列出客户端具备而网页版没有的能力：
- **自定义后端服务器地址**（OOBE 引导 + 服务器设置），支持对接任意 DNAISLAND 实例。
- **外观自定义**：任意取强调色（`flutter_colorpicker`）、明暗主题切换。
- **离线/在线大图查看**与滑动退出手势（`extended_image`）。
- **原生返回/预测性返回**、横屏侧边栏常驻布局。
- 关注 / 粉丝 / 我的收藏 / 我的点赞的**原生移动列表**与下拉刷新、无限滚动。
- 网络请求自动重试与连接池、请求错误日志（`ApiClient`）。

---

## 四、后端已就绪、等待客户端接入的接口（客户端可直接复用）
这些 `/api/v1/*` 接口后端已实现且逻辑与网页共用，客户端只要写 UI 即可打通：

| 能力 | 后端接口（已存在） | 客户端现状 |
| --- | --- | --- |
| 茶馆全套 | `/api/v1/teahouse/*`（feed/发帖/回复/点赞/收藏/话题/收藏/编辑/删除/搜索/卡关联） | ✅ 已接入 |
| 全站搜索 | `/api/v1/cards/search`、`/users/search`、`/teahouse/search` | ✅ 已接入 |
| 发布角色卡 | `POST /api/v1/cards/publish`、`/cards/import/parse` | ✅ 已接入 |
| 编辑/重新提审 | `POST /api/v1/cards/<id>/edit`、`/resubmit` | ✅ 已接入 |
| 我的角色卡 | `GET /api/v1/my/cards` | ✅ 已接入（管理页） |
| 图片上传 | `POST /api/v1/upload/image`、`/me/avatar` | 部分（发布/资料/工单内嵌 base64 图，未用独立上传接口） |
| 资料编辑 | `GET/POST /api/v1/me/profile` | ✅ 已接入（编辑页） |
| 用户搜索 | `GET /api/v1/users/search` | ✅ 已接入（搜索页） |
| 我的工单 | `/api/v1/tickets*`（列表/类别/新建/详情/回复/关闭/重开） | ✅ 已接入 |
| 举报元数据 | `GET /api/v1/reports/meta`（已接入卡详情） | ✅ 已接入 |
| 表情包 | `/stickers/api` + `/stickers/file/<code>`（公开 JSON） | ✅ 已接入（选择器 + 内联渲染） |
| 生图 | `/api/v1/image-gen/*`（meta/generate/tasks/logs） | ✅ 已接入（生图工作台） |

---

## 五、建议的补齐优先级（仅供参考）

> 茶馆、搜索、上传/发布、我的角色卡管理、个人资料编辑、我的工单、搜索实时建议、表情包、BYOK 代理配置、外链中转、生图已在客户端实现并提交。
>
> **不再规划补齐**：站内注册（保持跳转网页版）、法律协议页（保持用浏览器打开网页协议页）。
